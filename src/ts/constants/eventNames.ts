// The theme wire contract (DEVELOPERS.md). Frozen additive-only from
// 1.0: these names are never renamed or repurposed — the plugin auto-updates
// from WP.org while themes update on their own cadence, so every version
// pairing has to keep working.
export const EVENT_OPEN = 'arts-lightbox:open'
export const EVENT_CHANGE = 'arts-lightbox:change'
export const EVENT_DESTROY = 'arts-lightbox:destroy'
