diff --git a/dist/photoswipe.esm.js b/dist/photoswipe.esm.js
index 0b278f640123c8f1fe8b4c10514e9a0a58c2b83f..5c56c7ca1189a48f080148664547dd987ef1d1d9 100644
--- a/dist/photoswipe.esm.js
+++ b/dist/photoswipe.esm.js
@@ -4877,13 +4877,17 @@ class Content {
 
   removePlaceholder() {
     if (this.placeholder && !this.keepPlaceholder()) {
-      // With delay, as image might be loaded, but not rendered
+      // With delay, as image might be loaded, but not rendered.
+      // Patched (upstream PR #2166, merged post-5.4.4, unreleased): sync the
+      // delay to the opening animation instead of a hardcoded 1000ms so long
+      // transitions don't lose the placeholder mid-animation.
+      const delay = (Number(this.instance.options?.showAnimationDuration) || 500) + 500;
       setTimeout(() => {
         if (this.placeholder) {
           this.placeholder.destroy();
           this.placeholder = undefined;
         }
-      }, 1000);
+      }, delay);
     }
   }
   /**
diff --git a/src/js/slide/content.js b/src/js/slide/content.js
index 7f1dc683714fd4488d1ba079d19f61dfd4c11b92..05055a578c3067ee40b967707f86a331755a32ba 100644
--- a/src/js/slide/content.js
+++ b/src/js/slide/content.js
@@ -49,13 +49,17 @@ class Content {
 
   removePlaceholder() {
     if (this.placeholder && !this.keepPlaceholder()) {
-      // With delay, as image might be loaded, but not rendered
+      // With delay, as image might be loaded, but not rendered.
+      // Patched (upstream PR #2166, merged post-5.4.4, unreleased): sync the
+      // delay to the opening animation instead of a hardcoded 1000ms so long
+      // transitions don't lose the placeholder mid-animation.
+      const delay = (Number(this.instance.options?.showAnimationDuration) || 500) + 500;
       setTimeout(() => {
         if (this.placeholder) {
           this.placeholder.destroy();
           this.placeholder = undefined;
         }
-      }, 1000);
+      }, delay);
     }
   }
 
